/*!
 * Nexa — TypeScript declarations for the SSR helpers.
 * This app's frontend uses the Nexa framework (NOT React):
 * https://github.com/skysegbr/Nexa — see docs/AI_SPEC.md for the full
 * AI-oriented reference.
 */
/**
 * Type declarations for /dist/nexa-server.js — the server-side rendering entry.
 */
export { renderToString, hydrate } from "./nexa.js";
